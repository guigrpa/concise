{
  "name": "concise-examples",
  "private": true,
  "version": "0.4.2",
  "author": "Guillermo Grau Panea",
  "license": "MIT",
  "scripts": {
    "compile": "yarn compile:clean && yarn compile:run",
    "compileWatch": "yarn compile:clean && yarn compile:runWatch",
    "compile:clean": "rm -rf ./lib",
    "compile:run": "../../node_modules/.bin/babel src -d lib --ignore \"**/__mocks__/**\",\"**/__tests__/**\"",
    "compile:runWatch": "yarn compile:run --watch"
  },
  "devDependencies": {
    "babel-runtime": "^6.23.0",
    "concise-types": "^0.4.2",
    "prettier": "^2.2.1"
  },
  "dependencies": {
    "sequelize": "^4.37.6",
    "sqlite3": "^4.0.0",
    "concise-yaml": "^0.4.2",
    "concise": "^0.4.2",
    "concise-json": "^0.4.2",
    "concise-pg": "^0.4.2",
    "concise-flow": "^0.4.2",
    "concise-graphql": "^0.4.2",
    "concise-diagram": "^0.4.2",
    "concise-sequelize": "^0.4.2",
    "concise-firebase": "^0.4.2"
  }
}
