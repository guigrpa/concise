{
  "name": "concise-examples",
  "private": true,
  "version": "0.4.5",
  "author": "Guillermo Grau Panea",
  "license": "MIT",
  "scripts": {
    "compile": "yarn compile:clean && yarn compile:run",
    "compileWatch": "yarn compile:clean && yarn compile:runWatch",
    "compile:clean": "rm -rf ./lib",
    "compile:run": "../../node_modules/.bin/babel src -d lib --ignore \"**/__mocks__/**\",\"**/__tests__/**\"",
    "compile:runWatch": "yarn compile:run --watch"
  },
  "devDependencies": {
    "babel-runtime": "^6.23.0",
    "concise-types": "^0.4.5",
    "prettier": "^2.2.1"
  },
  "dependencies": {
    "sequelize": "^4.37.6",
    "sqlite3": "^4.0.0",
    "concise-yaml": "^0.4.5",
    "concise": "^0.4.5",
    "concise-json": "^0.4.5",
    "concise-pg": "^0.4.5",
    "concise-flow": "^0.4.5",
    "concise-graphql": "^0.4.5",
    "concise-diagram": "^0.4.5",
    "concise-sequelize": "^0.4.5",
    "concise-firebase": "^0.4.5"
  }
}
